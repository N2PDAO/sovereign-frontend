const defaultState = {
};
const types = {
};
const creators = {
};

export { types, creators };
export default function system(state = defaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
